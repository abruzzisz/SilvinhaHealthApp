git config --global user.name abruzzisz
git config --global user.email abruzzijulia69@gmail.com

-- Criar um projeto React Native Expo
npx create-expo-app@latest SilvinhaHealthApp --template blank

-- Restaurar o projeto (Toda vez que fizer um clone)
cd SilvinhaHealthApp
npm i

--executar o projeto
npx expo start
npx expo start --android

-- instalando os pacotes
cd SilvinhaHealthApp
npx expo install expo-router react-native-safe-area-context react-nativne-screens expo-linking expo-constants expo-status-bar